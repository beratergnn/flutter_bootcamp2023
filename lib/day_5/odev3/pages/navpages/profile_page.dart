import 'package:flutter/material.dart';

class Odev3ProfilePage extends StatelessWidget {
  const Odev3ProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profile Page'),
    );
  }
}
